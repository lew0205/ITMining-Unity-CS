using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AwakeStart : MonoBehaviour
{

    void Awake()
    {
        Debug.Log("Start Awake Func!");
    }

    void Start()
    {
        Debug.Log("Start Start Func!");
    }

    // Update is called once per frame
    void Update()
    {   
        
    }
}
